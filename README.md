# FPL League App
A web app for Fantasy Premier League players to join monetized leagues, compete, and win payouts via M-Pesa.

## Setup
1. Install Node.js, Firebase CLI, and Git.
2. Run `npm install`.
3. Configure Firebase in `lib/firebase.js`.
4. Start the app: `npm run dev`.